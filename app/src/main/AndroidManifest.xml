<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="by.isb.an07">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AN07">
        <activity android:name=".HW1Activity" />
        <activity android:name=".HW2Activity" />
        <activity android:name=".HW3Activity" />
        <activity android:name=".HW4Activity" />
        <activity android:name=".HW5Activity" />
        <activity android:name=".HW5ActivityWelcome" />
        <activity android:name=".FlagsActivity" />
        <activity android:name=".AnimationActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>